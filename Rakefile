# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/lamb_chop.rb'

Hoe.new('LambChop', LambChop::VERSION) do |p|
  # p.rubyforge_name = 'LambChopx' # if different than lowercase project name
  p.developer('Joe Van Dyk', 'joe@pinkpucker.net')
end

# vim: syntax=Ruby
